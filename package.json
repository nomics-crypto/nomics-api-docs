{
  "name": "nomics-api-docs",
  "version": "1.0.0",
  "description": "Nomics API Documentation",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "dev": "redoc-cli serve spec.yaml --template=template.hbs --watch",
    "bundle": "redoc-cli bundle spec.yaml --template=template.hbs",
    "samples": "node ./generate_samples.js",
    "start": "redoc-cli serve spec.yaml --template=template.hbs --port=$PORT --ssr"
  },
  "dependencies": {
    "redoc-cli": "^0.6.1"
  },
  "devDependencies": {
    "@kyleshockey/js-yaml": "^1.0.1",
    "swagger-client": "^3.9.4"
  }
}
